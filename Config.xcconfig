// Base config for the Wealth target. Real API keys live in
// Secrets.local.xcconfig, which is gitignored; the optional include below
// means the project still builds without it, and the app then fails loudly at
// the point a key is needed rather than degrading silently (§8).
#include? "Secrets.local.xcconfig"
