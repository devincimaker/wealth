import SwiftUI

struct MonthsView: View {
    var body: some View {
        ZStack {
            Color.wBackground.ignoresSafeArea()
            Text("Months")
                .foregroundStyle(Color.wTextSecondary)
        }
    }
}
