import SwiftUI

struct SubscriptionsView: View {
    var body: some View {
        ZStack {
            Color.wBackground.ignoresSafeArea()
            Text("Subscriptions")
                .foregroundStyle(Color.wTextSecondary)
        }
    }
}
