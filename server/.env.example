# Shared secret the app sends as Authorization: Bearer <token>.
WEALTH_API_TOKEN=

# Whisper transcription.
OPENAI_API_KEY=

# Claude subscription auth (create with: claude setup-token).
# Unset in dev = the local Claude Code login is used.
CLAUDE_CODE_OAUTH_TOKEN=

# Optional overrides.
# WEALTH_MODEL=claude-opus-5
# WEALTH_EFFORT=low
# PORT=3002
